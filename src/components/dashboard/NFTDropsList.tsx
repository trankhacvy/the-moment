import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/Tabs"

type NFTDropsListProps = {}

export const NFTDropsList = (props: NFTDropsListProps) => {
  return (
    <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
      
    </div>
  )
}
